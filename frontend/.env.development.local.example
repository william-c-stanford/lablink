# Copy this file to .env.development.local and fill in your local overrides.
# .env.development.local is git-ignored and takes precedence over .env.development.

# Override backend URL (e.g. if running in Docker or a remote devbox)
# VITE_API_BASE_URL=http://localhost:8000

# Override the dev-server port
# VITE_PORT=5173
