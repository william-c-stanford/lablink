# Database Schema — LabLink

<!-- garden-managed: auto -->
<!-- last-reviewed: 2026-03-09 -->

> Auto-generated from `src/lablink/models/`. Do not edit by hand — regenerated by `/context-gardening:tend`.
> ORM: SQLAlchemy 2.0 (async). Dev DB: SQLite. Production DB: PostgreSQL.

## Overview

| Stat | Value |
|---|---|
| Total models | 17 |
| Composite-PK association tables | 2 (ExperimentUpload, ExperimentPredecessor) |
| Models with soft delete | 3 (Organization, User, ApiToken) |
| State machines | 2 (Experiment, Campaign) |
| Immutable tables | 1 (AuditEvent — append-only, hash-chained) |

---

## organizations

Tenant root. All other entities are scoped to an organization.

| Column | Type | Constraints |
|---|---|---|
| `id` | String(36) | PK, default uuid4 |
| `name` | String(255) | NOT NULL |
| `slug` | String(100) | NOT NULL, UNIQUE, INDEX |
| `tier` | String(20) | NOT NULL, default "free" |
| `storage_limit_bytes` | BigInteger | NOT NULL, default 5 GB |
| `instrument_limit` | Integer | NOT NULL, default 2 |
| `user_limit` | Integer | NOT NULL, default 1 |
| `created_at` | DateTime(tz) | NOT NULL |
| `updated_at` | DateTime(tz) | NOT NULL |
| `deleted_at` | DateTime(tz) | nullable (soft delete) |

**Enum** `Tier`: `free`, `starter`, `professional`, `enterprise`

---

## users

| Column | Type | Constraints |
|---|---|---|
| `id` | String(36) | PK, default uuid4 |
| `email` | String(255) | NOT NULL, UNIQUE, INDEX |
| `password_hash` | String(255) | NOT NULL |
| `full_name` | String(255) | NOT NULL |
| `is_active` | Boolean | NOT NULL, default True |
| `created_at` | DateTime(tz) | NOT NULL |
| `deleted_at` | DateTime(tz) | nullable (soft delete) |

---

## memberships

M2M between users and organizations.

| Column | Type | Constraints |
|---|---|---|
| `id` | String(36) | PK, default uuid4 |
| `user_id` | String(36) | FK→users.id CASCADE, INDEX |
| `organization_id` | String(36) | FK→organizations.id CASCADE, INDEX |
| `role` | String(20) | NOT NULL, default "scientist" |
| `created_at` | DateTime(tz) | NOT NULL |

**Enum** `MemberRole`: `admin`, `scientist`, `viewer`
**Unique**: `(user_id, organization_id)` → `uq_membership_user_org`

---

## projects

Logical grouping of instruments and experiments within an org.

| Column | Type | Constraints |
|---|---|---|
| `id` | String(36) | PK, default uuid4 |
| `organization_id` | String(36) | FK→organizations.id CASCADE, INDEX |
| `name` | String(255) | NOT NULL |
| `description` | Text | nullable |
| `created_by` | String(36) | FK→users.id |
| `created_at` | DateTime(tz) | NOT NULL |
| `archived_at` | DateTime(tz) | nullable |

**Index**: `(organization_id, name)` → `ix_projects_org_name`

---

## instruments

Lab instrument registration.

| Column | Type | Constraints |
|---|---|---|
| `id` | String(36) | PK, default uuid4 |
| `organization_id` | String(36) | FK→organizations.id CASCADE, INDEX |
| `name` | String(255) | NOT NULL |
| `instrument_type` | String(100) | NOT NULL |
| `manufacturer` | String(255) | nullable |
| `model` | String(255) | nullable |
| `serial_number` | String(255) | nullable |
| `location` | String(255) | nullable |
| `agent_id` | String(36) | FK→agents.id SET NULL, nullable, INDEX |
| `metadata` | JSON | nullable |
| `created_at` | DateTime(tz) | NOT NULL |

**Index**: `(organization_id, instrument_type)` → `ix_instruments_org_type`

---

## agents

Desktop file-watcher agents registered by labs.

| Column | Type | Constraints |
|---|---|---|
| `id` | String(36) | PK, default uuid4 |
| `organization_id` | String(36) | FK→organizations.id CASCADE, INDEX |
| `name` | String(255) | NOT NULL |
| `api_key_hash` | String(255) | NOT NULL |
| `platform` | String(20) | nullable |
| `version` | String(50) | nullable |
| `last_heartbeat_at` | DateTime(tz) | nullable |
| `status` | String(20) | NOT NULL, default "active" |
| `config` | JSON | nullable |
| `created_at` | DateTime(tz) | NOT NULL |

**Index**: `(organization_id, status)` → `ix_agents_org_status`
**Valid statuses**: `active`, `inactive`, `offline`

---

## uploads

Central entity for raw instrument file ingestion.

| Column | Type | Constraints |
|---|---|---|
| `id` | String(36) | PK, default uuid4 |
| `organization_id` | String(36) | FK→organizations.id CASCADE, INDEX |
| `project_id` | String(36) | FK→projects.id SET NULL, nullable |
| `instrument_id` | String(36) | FK→instruments.id SET NULL, nullable |
| `agent_id` | String(36) | FK→agents.id SET NULL, nullable |
| `uploaded_by` | String(36) | FK→users.id SET NULL, nullable |
| `filename` | String(500) | NOT NULL |
| `content_hash` | String(64) | NOT NULL (SHA-256) |
| `file_size_bytes` | BigInteger | NOT NULL |
| `s3_key` | String(500) | NOT NULL |
| `mime_type` | String(100) | nullable |
| `status` | String(20) | NOT NULL, default "uploaded" |
| `error_message` | Text | nullable |
| `instrument_type_detected` | String(100) | nullable |
| `parser_used` | String(100) | nullable |
| `created_at` | DateTime(tz) | NOT NULL |
| `parsed_at` | DateTime(tz) | nullable |
| `indexed_at` | DateTime(tz) | nullable |

**Enum** `UploadStatus`: `uploaded`, `parsing`, `parsed`, `parse_failed`, `indexed`
**Unique**: `(organization_id, content_hash)` → `uq_uploads_org_hash` (deduplication)
**Indexes**: `(organization_id, status)`, `(organization_id, content_hash)`

---

## parsed_data

Canonical JSON output from instrument parsers; 1:1 with an upload.

| Column | Type | Constraints |
|---|---|---|
| `id` | String(36) | PK, default uuid4 |
| `upload_id` | String(36) | FK→uploads.id CASCADE |
| `organization_id` | String(36) | FK→organizations.id CASCADE, INDEX |
| `instrument_type` | String(100) | NOT NULL |
| `parser_version` | String(50) | NOT NULL |
| `measurement_type` | String(100) | nullable |
| `sample_count` | Integer | nullable |
| `data_summary` | JSON | NOT NULL |
| `measurements` | JSON | NOT NULL (list of MeasurementValue objects) |
| `units` | JSON | nullable |
| `instrument_settings` | JSON | nullable |
| `metadata` | JSON | NOT NULL |
| `created_at` | DateTime(tz) | NOT NULL |

**Indexes**: `(upload_id)`, `(organization_id, instrument_type)`

---

## experiments

Scientific experiments with state-machine lifecycle.

| Column | Type | Constraints |
|---|---|---|
| `id` | String(36) | PK, default uuid4 |
| `organization_id` | String(36) | FK→organizations.id CASCADE, INDEX |
| `project_id` | String(36) | FK→projects.id SET NULL, nullable, INDEX |
| `campaign_id` | String(36) | FK→campaigns.id SET NULL, nullable, INDEX |
| `intent` | Text | NOT NULL |
| `hypothesis` | Text | nullable |
| `status` | String(20) | NOT NULL, default "planned", INDEX |
| `parameters` | JSON | nullable |
| `constraints` | JSON | nullable |
| `outcome` | JSON | nullable |
| `design_method` | String(100) | nullable |
| `design_agent` | String(100) | nullable |
| `created_by` | String(36) | FK→users.id SET NULL, nullable |
| `created_at` | DateTime(tz) | NOT NULL |
| `started_at` | DateTime(tz) | nullable |
| `completed_at` | DateTime(tz) | nullable |

**Enum** `ExperimentStatus`: `planned`, `running`, `completed`, `failed`, `cancelled`
**State machine**: `planned → running → completed | failed | cancelled`
**Indexes**: `(organization_id, status)`, `(campaign_id)`

---

## experiment_uploads

M2M association: experiments ↔ uploads.

| Column | Type | Constraints |
|---|---|---|
| `experiment_id` | String(36) | FK→experiments.id CASCADE, PK |
| `upload_id` | String(36) | FK→uploads.id CASCADE, PK |
| `linked_by` | String(36) | FK→users.id SET NULL, nullable |
| `linked_at` | DateTime(tz) | NOT NULL |

**Composite PK**: `(experiment_id, upload_id)`

---

## experiment_predecessors

DAG edges for experiment lineage.

| Column | Type | Constraints |
|---|---|---|
| `experiment_id` | String(36) | FK→experiments.id, PK |
| `predecessor_id` | String(36) | FK→experiments.id, PK |

**Composite PK**: `(experiment_id, predecessor_id)`
**Unique**: `uq_experiment_predecessor`

---

## campaigns

Groups of related experiments (e.g., optimization runs).

| Column | Type | Constraints |
|---|---|---|
| `id` | String(36) | PK, default uuid4 |
| `organization_id` | String(36) | FK→organizations.id CASCADE, INDEX |
| `project_id` | String(36) | FK→projects.id SET NULL, nullable, INDEX |
| `name` | String(255) | NOT NULL |
| `objective` | Text | nullable |
| `status` | String(20) | NOT NULL, default "active", INDEX |
| `optimization_method` | String(100) | nullable |
| `created_by` | String(36) | FK→users.id SET NULL, nullable |
| `created_at` | DateTime(tz) | NOT NULL |

**Enum** `CampaignStatus`: `active`, `paused`, `completed`
**State machine**: `active → paused | completed; paused → active | completed`
**Index**: `(organization_id, status)`

---

## api_tokens

API tokens for users and agents (hashed at creation, not stored plain).

| Column | Type | Constraints |
|---|---|---|
| `id` | String(36) | PK, default uuid4 |
| `organization_id` | String(36) | FK→organizations.id CASCADE, INDEX |
| `created_by` | String(36) | FK→users.id CASCADE |
| `name` | String(255) | NOT NULL |
| `token_hash` | String(255) | NOT NULL, UNIQUE, INDEX (SHA-256) |
| `scope` | String(20) | NOT NULL, default "read" |
| `identity_type` | String(20) | NOT NULL, default "user" |
| `expires_at` | DateTime(tz) | nullable |
| `last_used_at` | DateTime(tz) | nullable |
| `is_active` | Boolean | NOT NULL, default True |
| `created_at` | DateTime(tz) | NOT NULL |
| `deleted_at` | DateTime(tz) | nullable (soft delete) |

**Enum** `TokenScope`: `read`, `write`, `admin`
**Enum** `IdentityType`: `user`, `agent`, `integration`

---

## audit_events

Immutable, append-only audit trail with SHA-256 hash chain.

| Column | Type | Constraints |
|---|---|---|
| `id` | String(36) | PK, default uuid4 |
| `organization_id` | String(36) | FK→organizations.id CASCADE, INDEX |
| `actor_type` | String(20) | NOT NULL (`user`, `agent`, `system`) |
| `actor_id` | String(36) | nullable |
| `action` | String(100) | NOT NULL (e.g., `upload.created`) |
| `resource_type` | String(50) | NOT NULL |
| `resource_id` | String(36) | NOT NULL |
| `details` | JSON | nullable |
| `ip_address` | String(45) | nullable |
| `user_agent` | Text | nullable |
| `hash` | String(64) | NOT NULL (SHA-256 chain integrity) |
| `created_at` | DateTime(tz) | NOT NULL |

**Indexes**: `(organization_id, resource_type, resource_id)`, `(organization_id, created_at)`, `(organization_id, actor_type, actor_id)`
**Never updated or deleted** — `hash` links each row to its predecessor for tamper detection.

---

## webhooks

Outbound webhook endpoint registrations.

| Column | Type | Constraints |
|---|---|---|
| `id` | String(36) | PK, default uuid4 |
| `organization_id` | String(36) | FK→organizations.id CASCADE, INDEX |
| `url` | String(2048) | NOT NULL |
| `secret` | String(255) | NOT NULL (HMAC-SHA256 signing secret) |
| `events` | JSON | NOT NULL (list of subscribed event types) |
| `is_active` | Boolean | NOT NULL, default True |
| `created_by` | String(36) | FK→users.id CASCADE |
| `created_at` | DateTime(tz) | NOT NULL |

**Supported events**: `upload.completed`, `parsing.completed`, `parsing.failed`, `experiment.created`, `experiment.status_changed`, `experiment.completed`, `agent.connected`, `agent.disconnected`

---

## webhook_deliveries

Per-event delivery attempts with retry tracking.

| Column | Type | Constraints |
|---|---|---|
| `id` | String(36) | PK, default uuid4 |
| `webhook_id` | String(36) | FK→webhooks.id CASCADE, INDEX |
| `event_type` | String(100) | NOT NULL |
| `payload` | JSON | NOT NULL |
| `status` | String(20) | NOT NULL, default "pending" |
| `attempts` | Integer | NOT NULL, default 0 |
| `last_attempt_at` | DateTime(tz) | nullable |
| `response_status` | Integer | nullable (HTTP status code) |
| `response_body` | Text | nullable |
| `created_at` | DateTime(tz) | NOT NULL |

**Enum** `DeliveryStatus`: `pending`, `delivered`, `failed`
**Max attempts**: 5 (exponential backoff)
